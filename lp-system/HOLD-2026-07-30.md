# HOLD — validation flags, 2026-07-30

Flagged rows keep their last verified value and honest older stamp. Publish clean rows only. Agents append arbitration notes below flags; validate.py re-runs append new sections and never rewrite earlier ones.

## validate.py run 2026-07-30T00:24:45 — 6 flag(s)

- **i0003** `price_interior`: moved +32% ($1052 -> $1391); needs source evidence, hold
- **i0003** `date_checked`: price changed but stamp is '2026-07-29', not today
- **i0004** `price_interior`: moved +36% ($1365 -> $1850); needs source evidence, hold
- **i0004** `date_checked`: price changed but stamp is '2026-07-29', not today
- **i0001** `provenance`: feeds p001 but is marked VERIFY-BEFORE-PUBLISH
- **p001** `inventory`: /en/go/itineraries/7-night-bahamas-perfect-day-from-new-york/ has 0 publishable deal rows for filter 'itin:7n-bahamas-perfect-day-cape-liberty', needs >=1 (1 matching row(s) blocked on provenance)

## Mobile-polish final gate (qa-auditor, 2026-07-30, audit #9) — pages pass; operator inputs still block live

Pages (rebakes of 2026-07-30 00:23, = the exact files tracking-guardian re-baselined in audit #8):
- p002 lp-system/out/preview/en/go/lines/royal-caribbean/from/galveston/index.html (96,006 B)
- p007 lp-system/out/preview/en/go/itineraries/5-night-western-caribbean-from-galveston-mariner-of-the-seas/index.html (72,996 B)
- p008 lp-system/out/preview/en/go/ships/mariner-of-the-seas/index.html (71,017 B)

### validate.py (run at audit open, exit 1, output verbatim)

    VALIDATE: 6 flag(s) -> /Users/lokesh29/cruiselineadvisors/lp-system/HOLD-2026-07-30.md
      FLAG i0003 price_interior: moved +32% ($1052 -> $1391); needs source evidence, hold
      FLAG i0003 date_checked: price changed but stamp is '2026-07-29', not today
      FLAG i0004 price_interior: moved +36% ($1365 -> $1850); needs source evidence, hold
      FLAG i0004 date_checked: price changed but stamp is '2026-07-29', not today
      FLAG i0001 provenance: feeds p001 but is marked VERIFY-BEFORE-PUBLISH
      FLAG p001 inventory: /en/go/itineraries/7-night-bahamas-perfect-day-from-new-york/ has 0 publishable deal rows for filter 'itin:7n-bahamas-perfect-day-cape-liberty', needs >=1 (1 matching row(s) blocked on provenance)

All 6 are pre-existing or mechanical: i0003/i0004 price flags are re-flags of
holds arbitrated CONFIRMED-AT-SOURCE on 2026-07-29; the two NEW date_checked
flags are date-rollover artifacts of those same rows (the calendar moved to
07-30 while the stamps honestly say 07-29; no data changed since the
arbitration, CSV verified); i0001/p001 are the open Cape Liberty seed items
outside this slice. No new fact rows changed in this bake (template/JS-only +
the deposit_note restructuring, values verbatim, see below), so no new 10%
price sample was due; the audit-6 full 16/16 fresh-source comparison stands.

### New standing mobile check: no dialog/sheet visible on initial load — PASS all three

CSS verified in all three bakes: `.sheet.off{transform:translateY(105%);
visibility:hidden;pointer-events:none;transition:transform .2s ease,
visibility 0s .2s}` and `.sheetback.off{opacity:0;visibility:hidden;
pointer-events:none;transition:opacity .2s ease,visibility 0s .2s}`; base
`.sheet`/`.sheetback` rules keep `visibility 0s` so opening shows instantly.
Mechanism audited: `.sheet` sets display:flex (author CSS overrides the UA
[hidden] rule), so the markup-baked `class="sheet off"` is what actually
hides it on load; the backdrop sets no display, so its `hidden` attr holds
until JS removes it and only ever alongside removing `.off`; sheetClose()
re-adds `.off` then re-hides on transition end (with the 240ms fallback).
Coordinator's real-Chrome captures cited: computed visibility:hidden on load
at BOTH 390 and 1440 on all three pages; elementFromPoint at the bottom edge
resolves to sticky-cta/content, never sheet or backdrop; scrollWidth exactly
390/1440.

### Two-screens-to-content at 390 — PASS (method verified, p008 spot-checked)

Method sound: real Chrome at 390x844, budget 2x844=1688px; measured first
content 1652 (p002) / 1278 (p007) / 1330 (p008). p008 structural spot-check
from the baked CSS corroborates: pre-content stack = collapsed one-line
disclosure (~33px) + 60px nav + 420px mobile hero + 14px callzone margin
= ~527px fixed, leaving ~800px for the compacted callzone card + intro,
consistent with a card holding h2, callrows, bigcall and the COLLAPSED
callback button. Compaction verified in markup: hours badge one line at
<=480 (the "Advisors answering now · " prefix is a separate span that hides;
"8am-11pm ET, every day" ALWAYS renders — no 24/7 claim, real hours always
visible), hero 260px on p002, offerbar flex-wrap.

### Coral discipline — PASS both viewports, all three

CSS inventory (media-query-aware rule extraction): the coral tokens
(--coral/--coral-d, hex only in :root) are consumed by exactly ONE class,
`.btn`. Every `.btn` in markup was mapped to its ancestor: coral survives
only on (a) sticky bar call button, (b) hero CTA (p007 line 390, p008
line 353 — p002 has none), (c) sheet-footer CTAs (built only when a sheet
opens). All others are overridden white/navy: `.cabin-panel .btn,
.noresults .btn` (p002), `.callrow .btn,.ship-panel .btn,.fare-cta` (p007),
`.callrow .btn` (p008), `.sticky-cta .cb-btn` navy, `.sheet-foot .btn2`
white. Judgment calls ACCEPTED: phoneband bigcall is transparent with a
WHITE border on the navy band (navy-on-navy would be invisible; it is not
coral); offer ribbon is a soft gold tint (#FFF7EC + gold border, navy text),
informational, not a CTA. Matches the real-Chrome per-844px-band counts
1/2/2 max — all <=2.

### Polish items — spot-check results

- Header icon button <=480: verified on all three (42px circle,
  navy-outline, `.cta-t` hidden / `.cta-i` shown, tel: link). p002
  EXTENSION ACCEPTED: identical two-line collision once the real brand
  baked; DoD nowhere requires a text CTA in the header, phone count stays
  >=5, and the icon stays a tracked tel: link (guardian: tel_click fires,
  position 'other', documented).
- Hero fare nowrap: `.hero .fareline .a{white-space:nowrap}` on p007/p008;
  heroFare fills "$398 pp" as ONE text node inside `.a`, so amount+unit
  cannot break.
- Disclosure: one-line collapsed summary button; FULL ribbon text always in
  the static DOM (`#discloseFull`); hiding gated on JS-added `.disclose.js`
  so no-JS renders the full legally load-bearing text. Verified all three.
- Collapsed callback: `.cb.js:not(.open)` hides form+h3 behind the navy
  "Prefer we call you?" button, mobile only; lead_submit alive after expand
  cited from guardian audits #7/#8 (fires exactly once, form_mode
  placeholder, ids unchanged).
- p002 one-line rows: at <=900 rows hide meta/ribbon/dd/months/stamp/go/
  more and show 44px thumb + truncated name + "N nts" chip + from-price;
  first 6 rows + dynamic "Show all ${list.length} sailings" navy expander
  (re-renders per search, hidden once .allrows); row tap opens the deal
  sheet; featured slider capped `.slice(0,4)` with per-slide "seen" stamps.
- STAMP-COVERAGE RULING (p002 mobile rows): **ACCEPTED, with a condition.**
  Every visible row from-price sits directly under the results-head line
  ("N itineraries · from-fares by month · seen July 29"), which has no
  mobile hiding rule and re-renders with every search; the deal sheet a row
  opens carries its own "From-fares seen July 29" sstamp; and every one of
  the 16 baked rows shares date_checked 2026-07-29 (CSV verified), so the
  single list-level date is accurate for every price shown. The
  every-price-stamped rule is satisfied. CONDITION / GUARD NOTE (N1,
  page-builder, non-blocking today): generate.py stamps pricesCheckedOn
  from the NEWEST date_checked shown and p002 uses that one const for all
  rows (desktop per-row stamps included). A future mixed-date bake would
  overstate freshness on older rows. Switch to per-row stamps (rates pages
  already bake per-deal "stamp") or make the bake fail when row dates
  diverge.
- Deposits mini-tables: p007 BOOKING.deposit and p008 INCLUDED.deposit are
  IDENTICAL structs, checked cell-by-cell against 01_lines.csv rci
  deposit_note: 4 deposit tiers ($100/1-5, $250/6-9, $450/10-14, $500/15+
  nights pp), 3 final-payment rows (75/1-4, 90/5-14, 120/15+ nights), and
  the Bermuda-$250 / suite-10% prose leftover, all values verbatim. One
  mechanical normalization: "due 75 days out" renders "75 days before
  sailing" — no number touched, sentence ours; accepted. Sources
  source_deposit = royalcaribbean.com FAQ URLs (whitelisted), carried from
  the audit-6 registrar fix.
- Fare table (p007): section bakes `class="fares mode-i"`, so mobile shows
  exactly month + ONE fare column per toggle state (mode-i hides col 3,
  mode-b hides col 2, per-row CTA col 4 hidden); segmented control toggles
  the class only; every fare cell renders `$X` + its own `.st` "pp · seen
  July 29" stamp INSIDE the cell, so stamps stay attached in both states;
  null fares render "Call for fare" (no unstamped number possible); single
  navy `.fare-cta` sits inside `section.fares` (guardian re-proved
  tel_click position deal-card by click-through). Desktop keeps the 4-col
  table (fareseg + fare-cta display:none outside <=900).
- Route ribbon (p007): REMOVED — zero heroRoute/routeline/routeOf
  occurrences; replacement comment sits at the hero; day slider + own-SVG
  schematic remain.
- Kickers: `.kicker{display:none}` at <=900 on all three; p002 exception
  `.featwrap .kicker` ACCEPTED (the featured slider's only caption; not a
  density violation).

### Tracking gate (cited, not redone)

tracking-guardian audit #7 PASS (95/95 new-surface checks; zero drift on
the polish bake; lead_submit alive after expand; fare-cta -> deal-card
re-proven; new buttons emit no events) and audit #8 CLEAN PASS on these
exact bakes: T-ARIA-DNI fix verified 15/15 (staleAria=0/swappedAria=1,
MutationObserver re-applies aria+href+text, non-number aria-labels
untouched, empty-Google fallback safe), drift reduction-proven to be
exactly the aria pass, fingerprints re-baselined (p002 bec631ef, p007
664f0270, p008 9a45c5e0). qa independently verified the aria pass is
present in all three baked DNI applyAll blocks and that the nav cta is the
ONLY aria-label on each page carrying the phone number.

### Standard sweep (all three)

noindex,nofollow present; no sitemap anywhere in the preview tree; ZERO
inbound /go/ links from site/ or newsite/dist; §6a "Fares shown" paragraph
present; disclosure ribbon top naming Royal Caribbean; TCPA fine print
linking /en/legal/consent/ (x2 each); sticky call bar; zero em dashes;
zero IMG SLOT / [AGENCY NAME] / +18885550142 / raw asset: strings; phone
display 8/9/12 + tel links 7/10/11 (>=5); weights 96.0/73.0/71.0 KB
(<150KB excl. images); all stamps July 29 (1 day old, <=10); o002 banner
within its verified window (ends 2026-07-31; auto-expiry paths verified in
earlier audits). Carried operator blockers UNCHANGED and expected:
placeholder phone + #CALLBACK_ACTION (x2/page) + §6a taxes wording.

### Verdicts (audit #9, mobile-polish final gate)

- p002: **APPROVE-PENDING-OPERATOR-INPUTS** — all polish, mobile-standing,
  DoD, provenance and tracking checks pass; blocked from live solely by
  the three operator items.
- p007: **APPROVE-PENDING-OPERATOR-INPUTS** — same basis.
- p008: **APPROVE-PENDING-OPERATOR-INPUTS** — same basis.

OPERATOR INPUTS required before any page goes live (unchanged, 3 items):
1. Real tracking phone number (placeholder 1-833-555-0100 in every slot
   incl. all three meta descriptions and the DNI REAL_DISPLAY constant).
2. Real callback form endpoint (action="#CALLBACK_ACTION" discards leads).
3. §6a taxes-wording ruling (footer says fares "exclude taxes, fees and
   port expenses" while every rendered Galveston fare is taxes-included at
   source).
Plus GTM-side setup per tracking-access.md (guardian
PENDING-LIVE-VERIFICATION) at go-live.

Non-blocking notes riding forward: N1 above (global pricesCheckedOn vs
mixed row dates); validate.py should consume arbitrations (i0003/i0004
re-flag every run, now also as date_checked rollover artifacts); M2
real-device touch confirmation of swipe-down/scroll-snap at the
post-operator-input final gate.

## validate.py run 2026-07-30T11:09:42 — 6 flag(s)

- **i0003** `price_interior`: moved +32% ($1052 -> $1391); needs source evidence, hold
- **i0003** `date_checked`: price changed but stamp is '2026-07-29', not today
- **i0004** `price_interior`: moved +36% ($1365 -> $1850); needs source evidence, hold
- **i0004** `date_checked`: price changed but stamp is '2026-07-29', not today
- **i0001** `provenance`: feeds p001 but is marked VERIFY-BEFORE-PUBLISH
- **p001** `inventory`: /en/go/itineraries/7-night-bahamas-perfect-day-from-new-york/ has 0 publishable deal rows for filter 'itin:7n-bahamas-perfect-day-cape-liberty', needs >=1 (1 matching row(s) blocked on provenance)

## validate.py run 2026-07-30T11:09:52 — 6 flag(s)

- **i0003** `price_interior`: moved +32% ($1052 -> $1391); needs source evidence, hold
- **i0003** `date_checked`: price changed but stamp is '2026-07-29', not today
- **i0004** `price_interior`: moved +36% ($1365 -> $1850); needs source evidence, hold
- **i0004** `date_checked`: price changed but stamp is '2026-07-29', not today
- **i0001** `provenance`: feeds p001 but is marked VERIFY-BEFORE-PUBLISH
- **p001** `inventory`: /en/go/itineraries/7-night-bahamas-perfect-day-from-new-york/ has 0 publishable deal rows for filter 'itin:7n-bahamas-perfect-day-cape-liberty', needs >=1 (1 matching row(s) blocked on provenance)

---

## Operator design batch + bugfix gate (qa-auditor, 2026-07-30, audit #10) — VERDICT: HOLD on all three

Files audited (bakes of 2026-07-30 11:08, i.e. the rebake made AFTER
tracking-guardian audit #9 which covered the 10:01-10:28 bakes):

- p002 lp-system/out/preview/en/go/lines/royal-caribbean/from/galveston/index.html (100,550 B)
- p007 lp-system/out/preview/en/go/itineraries/5-night-western-caribbean-from-galveston-mariner-of-the-seas/index.html (75,122 B)
- p008 lp-system/out/preview/en/go/ships/mariner-of-the-seas/index.html (72,624 B)

### validate.py (run at audit open, exit 1, output verbatim)

    VALIDATE: 6 flag(s) -> /Users/lokesh29/cruiselineadvisors/lp-system/HOLD-2026-07-30.md
      FLAG i0003 price_interior: moved +32% ($1052 -> $1391); needs source evidence, hold
      FLAG i0003 date_checked: price changed but stamp is '2026-07-29', not today
      FLAG i0004 price_interior: moved +36% ($1365 -> $1850); needs source evidence, hold
      FLAG i0004 date_checked: price changed but stamp is '2026-07-29', not today
      FLAG i0001 provenance: feeds p001 but is marked VERIFY-BEFORE-PUBLISH
      FLAG p001 inventory: /en/go/itineraries/7-night-bahamas-perfect-day-from-new-york/ has 0 publishable deal rows for filter 'itin:7n-bahamas-perfect-day-cape-liberty', needs >=1 (1 matching row(s) blocked on provenance)

All 6 are the same pre-existing / date-rollover flags carried since audit #9
(i0003/i0004 arbitrated CONFIRMED-AT-SOURCE 2026-07-29; i0001/p001 are the
open Cape Liberty seed items outside this slice). No data CSV has been
modified since 2026-07-29 18:22 (verified by mtime + git status), so ZERO
fact rows changed in this batch and no new 10% price sample was due.

### BLOCKER D-A (p002, p007, p008) — the independent-service disclosure is NOT above the fold

This is the reason the top ribbon existed. Its replacement does not replace it.

Measured in real Chromium, initial load, no scrolling, `.sticky-cta` is
`position:fixed; z-index:70; background:rgb(255,255,255)` (opaque), so the
effective visible fold is the sticky bar's top edge:

| page | viewport | .ind-note top-bottom | sticky top | unobstructed px visible |
|---|---|---|---|---|
| p002 | 360x844 | 973-1033 | 745 | **0** |
| p002 | 390x844 | 951-991  | 745 | **0** |
| p002 | 430x844 | 925-965  | 770 | **0** |
| p007 | 360x844 | 821-881  | 745 | **0** |
| p007 | 390x844 | 799-839  | 745 | **0** |
| p007 | 430x844 | 773-813  | 770 | **0** |
| p008 | 360x844 | 863-923  | 770 | **0** |
| p008 | 390x844 | 841-881  | 770 | **0** |
| p008 | 430x844 | 815-855  | 770 | **0** |

Method: for every scanline of the note's box inside the viewport,
`document.elementFromPoint` was asked for the top-most element; the note was
never it. Also below the fold on desktop p002 at 1440x900 (864-904, vh 900).

What IS above the fold on load at 390x844, in reading order:

- p002: header phone `1-833-555-0100`, brand lockup, hero eyebrow
  "ROYAL CARIBBEAN · FROM GALVESTON, TEXAS", H1 "Royal Caribbean Cruises
  from Galveston", the finder, and the call zone h2. No disclosure.
- p007: eyebrow "ROYAL CARIBBEAN · FROM GALVESTON TX", H1 naming the line and
  the ship, **"$398 pp"** with its stamp, coral "Call for today's fare".
  No disclosure.
- p008: eyebrow "ROYAL CARIBBEAN · SHIP GUIDE", H1 "Mariner of the Seas...",
  **"$398 pp"** with its stamp, coral "Call for today's fare". No disclosure.

A visitor arriving from a paid Royal Caribbean brand-keyword ad sees a
Royal-Caribbean-branded page, a price and a phone number, and nothing that
says we are not Royal Caribbean, without scrolling. That is CLAUDE.md hard
rule 2 and the Google misrepresentation policy, on the exact traffic these
pages are built for. The placement itself is good (directly under the
tap-to-call button, correct wording, names the line, `visibility:visible`,
no collapse ancestor, in the static DOM, no JS gate) - it is simply ~100-210px
too low.

FIX REQUIRED: render `.ind-note` above the call-zone CTA stack (e.g. as the
first line of the call-zone card, or attached to the hero eyebrow), then
re-prove ZERO-SCROLL visibility at 360/390/430 x 844 with the fixed sticky
bar accounted for. Approval of this batch is void until that measurement is
re-run and shows non-zero unobstructed pixels on all three.

### BLOCKER D-B (p008 rendering; p007 latent) — the copy rewrite turned honest gaps into false claims about Royal Caribbean

The nine rewrites removed internal-sourcing register, but four of them
universalised a source-scoped gap into an assertion about the trademark
owner's publishing practices. Two are FALSE at that owner's own official,
whitelisted site, verified by me today.

Source opened 2026-07-30 (HTTP 200, 895,048 B):
`https://www.royalcaribbean.com/cruise-ships/mariner-of-the-seas/deck-plans`

1. p008 `#decksTxt` renders: "Royal Caribbean publishes venue lists rather
   than a deck-by-deck map, so we keep this simple and written..."
   **FALSE.** That page publishes a deck-by-deck plan for this ship;
   "Deck 02" through "Deck 15" are enumerated in the served document.
2. p008 `#casinoTxt` renders: "Royal Caribbean does not publish casino
   details for Mariner of the Seas, so we do not list any here."
   **FALSE.** The same page publishes "Casino Royale(SM)" as a Mariner venue
   with descriptive copy ("...your little slice of Vegas right at sea, with
   thousands of square feet of...") and venue imagery
   (`casino-royale/casino-royale-roulette-friends-gambling-slots.jpg`).
3. p008 `#showsBody` renders: "Show and nightlife schedules are set per
   sailing, and Royal Caribbean does not publish a fixed lineup for Mariner
   of the Seas, so we do not guess at one." Unsourced negative claim about a
   third party; neither confirmed nor refuted at source. Not publishable as
   written.
4. p007 night tab (latent - currently unreachable because `T.night.venues`
   has 4 entries): "Royal Caribbean does not itemize evening venues for this
   ship, so we do not guess." Same class of claim; would render for any ship
   whose bars data is empty.

Our own registry is honestly scoped and does NOT support these sentences.
`newsite/data/ships/royal-caribbean.json` -> Mariner -> `exp.casino.note`:
"Honest gap: no casino venue is stated on any loaded official Mariner page
(ship page, things-to-do, rooms, family/free-things/top-things guides) as of
2026-07-29. Only the fleet-wide Club Royale casino rewards program appears,
in site navigation. Left null rather than assumed."

That note says *we have not found it on the pages we loaded*. The page now
says *Royal Caribbean does not publish it*. Those are different claims, and
the second one is wrong. Hard rule 3 (never invent a fact) plus the
provenance policy (every rendered fact traces to a source URL) are both
breached, and it is an incorrect public statement about the brand we are
bidding on.

FIX DIRECTION (page-builder): keep the guest-facing register, drop the
third-party assertion; make the sentence about US, not about them. e.g.
"We do not have a verified casino listing for this ship, so we do not guess.
Ask on the call." Do not state what Royal Caribbean does or does not
publish anywhere on any page. This applies to all four strings and to any
future generator string of the same shape.

### BLOCKER D-C (p008) — fewer than 3 live deal rows

DoD requires ">=3 live deal rows render for the page's deal_filter". p008
renders 2 (`.scard` count = 2, both viewports). Only i0005 and i0008 carry
ship_id s016 in 03_itineraries.csv; no other Mariner row exists.

Prior audits (#6, #9) recorded p008 as passing all DoD structure checks.
That was not correct on this line and I am not carrying it forward ("never
approve on it-was-fine-last-week"). Resolution is not mine to pick: either
research-registrar collects further verified Mariner inventory (note her
Galveston deployment ends 2026-10-26 per the registrar's own verification
file, but a ship page is not port-scoped, so her later New Orleans / Europe
sailings are in scope), or structure-guard / the operator records an
explicit written waiver of the >=3 threshold for ship-type pages. Until one
of those exists, p008 fails this DoD line.

### Tracking gate

tracking-guardian audit #9 CLEAN PASS is cited, with one correction: the
audited files were the 10:01-10:28 bakes; the current files are 11:08
rebakes containing the Back-button fix. I therefore re-ran the drift work
myself rather than citing it blind.

- v1 fingerprint recipe replayed on the 11:08 bakes: **MATCHES the audit-#8
  baselines byte-for-byte** on all three (p002 bec631ef..., p007
  664f0270..., p008 9a45c5e0...). The rebake did not touch the tracking
  blocks.
- dataLayer-surface census invariant holds on all three: 6 script blocks /
  3 touching dataLayer / 3 `dataLayer.push()` sites / exactly 2 event names
  (tel_click, lead_submit).
- NAV_UI_JS supplemental fingerprint **MOVED**: 1073db9f... -> 7918ce7a...
  (identical across all three pages). That is the Back-button fix, which
  landed after guardian's audit. I audited the new block myself (static read
  + runtime, below). **tracking-guardian must re-baseline this supplemental
  hash**; the block remains tracking-inert (no dataLayer / gtag / clarity /
  _googWcmGet / tel: / href assignment; both controls are `<button>`).

### Back button (batch item 3) — fix verified, control ACCEPTED with a condition

Baked condition: `ref.indexOf(location.origin+'/')===0 && ref!==location.href`.
Verified in real Chromium over HTTP (127.0.0.1:8899 serving the preview tree):

| scenario | document.referrer | history.length | Back rendered |
|---|---|---|---|
| direct landing p002 | "" | 2 | **no** |
| direct landing p007 | "" | 2 | **no** |
| p007 with referer https://www.google.com/ (the paid-click case) | google.com | 2 | **no** |
| p007 with same-origin referer = p002 | p002 URL | 2 | yes |
| p008 with same-origin referer = p002 | p002 URL | 3 | yes |
| p007 with referer = itself | p007 URL | 4 | **no** |
| p007 with look-alike host referer (origin + ".evil.com") | stripped | 4 | **no** |
| real p002 -> p007 click, then Back click | - | - | returns to p002 |

`history.length` was 2 on a direct landing, i.e. the pre-fix condition would
indeed have rendered Back on a paid click. Fix confirmed. Insertion as
`.nav .wrap` firstChild does not disturb the brand lockup or the header tel
CTA; scrollWidth stays exactly 390. Button is 53x34 at top 53, left 20,
aria-label "Go back to the previous page".

RULING: an exit control in the header of a paid landing page is ACCEPTABLE
under this gate. It cannot return the visitor to the SERP or the ad; it is a
`<button>`, so there is no crawlable exit and no tel_click surface; and with
zero inbound /go/ links from the main site (re-verified this run) the only
referrer that can trigger it is another /go/ page, i.e. the destination is
always still inside the paid funnel. CONDITION (recommended hardening, one
line): tighten the gate from "same-origin" to "same-origin AND referrer path
starts with /en/go/", so that if a main-site page ever links in, Back does
not silently become a route out of the funnel. Leaving it unmeasured is
accepted for now, because the destination is always another LP.

### Passing checks (all three unless noted)

- Logo lockup (item 2): `SPAN.brand.brand-lockup`, inline SVG with
  `aria-hidden="true"` plus visible brand text, **not** wrapped in an `<a>`.
  No new exit link. Full exit-link inventory per page is legal pages only
  (+ the internal p007 link on p002/p008).
- Imagery (item 4): 32/32 baked image refs across the three pages resolve to
  an existing file in the preview tree, map to a 10_assets.csv row with
  `status=sourced` and a populated `license_note`, and carry alt text
  **verbatim** from the registry. Zero broken images at runtime (20/12/14
  `<img>` elements). Finder cards: 8 distinct assets across 16 cards with
  ZERO adjacent repeats (LRU rotation works). Ship-tab images on p002 carry
  "Representative image, not the specific ship."
- ddpulse: `@keyframes ddpulse` applied to `.daymore,.dd summary`, 3
  iterations, box-shadow only, and guarded by
  `@media(prefers-reduced-motion:reduce){.daymore,.dd summary{animation:none}}`.
  Absent on p008 (no day-by-day). PASS.
- Disclosure-bar removal is clean: ZERO occurrences of `disclose` of any kind
  (markup, CSS or JS) on all three - no orphaned DOM reference, so no
  exposure to CLAUDE.md pitfall #1.
- Ribbon bugfix: `overflow-x:auto`, `.pname` computed `max-width:120px`;
  on the 5-port sheet scrollWidth 439 vs clientWidth 350; **0 label overlaps
  and 0 clipped labels across all sheets opened**.
- Deal sheet day-by-day restored and at EXACT parity with the desktop
  expander: 7/6/6/5 day rows for the first four rows, same strings in the
  same order. Sheet keeps its own "From-fares seen July 29" stamp, 1 tel
  link, and page scrollWidth stays 390 with the sheet open.
- phoneband h2 is `rgb(255,255,255)` on the navy gradient. Fixed.
- No dialog/sheet visible on load: `.sheet` computed `visibility:hidden`,
  0 open dialogs, at 390 and 1440.
- Two-screens-to-content at 390: first content after the call zone at
  1198 (p002) / 1046 (p007) / 1088 (p008) vs the 1688 budget. Improved by
  the ribbon removal.
- Coral <=2 per 844px band: max 2 on all three.
- scrollWidth exactly 390 at 390px and 1440 at 1440px, sheets open included.
- noindex,nofollow present; no sitemap anywhere in the preview tree; ZERO
  inbound /go/ links from site/ or newsite/dist.
- Footer legal blocks are **byte-identical** to
  `newsite/legal_partial.py legal_blocks_html("en", include_fares_paragraph=True)`
  on all three, including the section 6a "Fares shown" paragraph. TCPA fine
  print links `/en/legal/consent/` (2 consent links per page). Zero `href="#"`.
- Weights 100,550 / 75,122 / 72,624 B, all < 150KB excluding images.
- Zero `[AGENCY NAME]` / `IMG SLOT` / `+18885550142` / raw `asset:` strings;
  zero duplicate IDs.
- Every rendered fare carries its "seen July 29" stamp (1 day old, <= 10).
- Offer o002 self-expires: `renderOffers()` filters on the client date
  against start/end, so the banner disappears after 2026-07-31.

### Non-blocking defects to fix in the same rebake

- **N-1 (p002)** The repnote reads "Representative image, not the specific
  ship. **Official photography coming.**" The second sentence is a
  forward-looking claim about obtaining official (i.e. cruise-line)
  photography, on a page whose entire posture is non-affiliation. Drop it;
  "Stock image, not the specific ship." says everything needed.
- **N-2 (all three)** One em dash survives, in the NAV_UI_JS comment
  ("header Back button — ONLY when the previous page is one of ours",
  p002 line 2446 / p007 1047 / p008 1107). Not rendered copy, but it breaks
  the previously certified zero-em-dash file state and any file-level guard.
- **N-3 (all three)** `.ind-note` is `#6B7C86` on `#FFFFFF` at 12.5px/600 =
  **4.33:1**, below WCAG AA (4.5:1) for normal text. This is the page's
  primary compliance disclosure; "clear and conspicuous" wants better than
  a near-miss. Darken it. (`.repnote` `#8B9AA4` = 2.90:1; lower stakes but
  also weak.)
- **N-4 (all three)** Back-to-top is 44x44 at `bottom:88px`, `z-66`; at 390px
  the sticky bar is **99px** tall (top 745), so the button's bottom 11px is
  painted over by the sticky bar (z-70). No conversion risk (the sticky bar
  wins the tap) but the control is clipped. Raise to ~`bottom:110px`.
  Guardian's clearance note assumed a ~67px sticky bar, which holds only at
  >=430px.
- **N-5 (all three)** The coordinator's "rendered-text sweep finds no
  internal-sourcing language" is not accurate. Three source-register strings
  still render: p002 "Royal Amplified (class not stated on official page)"
  (deliberate, audit-#6-approved registrar-verbatim honesty note - KEEP IT),
  p002 "Official photography coming." (N-1), and p008 "The official rooms
  page does not publish berth counts. It states that select staterooms
  connect..." (honest, correct, and safer than the D-B rewrites). Note the
  inconsistency: the pass universalised four claims about Royal Caribbean
  while leaving these. The correct direction is the reverse of what was done.
  Confirmed separately: the "Internal cross-check" sentence IS stripped from
  the rendered quad_note (0 occurrences on p007/p008).
- **N-6 (p007, p008)** The inline representative-image note exists only on
  p002's ship tabs. p008's hero is a generic ship-at-sea photo on a page
  titled "Mariner of the Seas" with no adjacent note, and p007's ship tabs
  use themed generic imagery. The footer Photography block covers this and
  audit #6 accepted it, but the inline note should be extended for
  consistency now that it exists.
- **N-8** Offer o002 carries `status=draft` in 07_offers.csv while rendering
  on a publishable page, and expires 2026-07-31. Self-expiry verified;
  confirm the draft status value is intentional.
- **N-9** `gtag is not defined` pageerror fires on ALL THREE pages, from the
  live GTM container GTM-NM78WCVF. This is the known main-site defect in
  tracking-access.md and the prime suspect for the inactive website-call
  conversion; it now reproduces on the LPs. It must be resolved in the
  container before any spend, or a tel_click may never reach an Ads
  conversion action.
- **N-10** p002 shows the phone number as visible text only 4 times at 390px
  (desktop 20; 23 tel: links after render). Passes the DoD on a link-based
  reading; recorded for the operator's awareness.

### N-7 PRICE DRIFT — required pre-deploy action (not a defect of this batch)

No fact row changed this run, so no 10% sample was due. I re-queried the
official source anyway (`https://www.royalcaribbean.com/graph` cruiseSearch,
`departurePort:GAL`, HTTP 200, fetched 2026-07-30; response cached at
`lp-system/out/cache/2026-07-29/` peers as scratchpad `qa10-gal-priced.json`)
and 3 of 3 spot-checked rows have moved within 24 hours:

- **i0005** (renders on all three pages): stored balcony **$566**, fresh
  package minimum **$703.87 (+24%)**; 2026-08 override stored i448/b598 vs
  fresh 443.68/713.68; 2026-10 stored i456/b794 vs fresh 474.14/768.12.
  Interior default $398 still matches ($398.37).
- **i0008**: defaults match to the dollar (fresh 459.48 / 707.48). Its
  2026-08 ($751) and 2026-09 ($603) overrides are NOT in today's pull (only
  2026-10 sailings remain in the package) -> **could not verify at source**
  today for those two months.
- **i0009**: stored i649/b806 vs fresh minimum 679.47/848.97 (mismatch);
  2026-09 b849 matches 848.97; 2026-08 b1029 vs fresh 1118.34 (mismatch).

Nothing published today is dishonest - every rendered fare is stamped
"seen July 29" and is 1 day old. But this inventory demonstrably reprices
overnight, so the 10-day stamp window must NOT be read as 10 days of
tolerance here. REQUIRED: a pricing-scout refresh on the day of deploy,
immediately before the bake that ships, followed by a placeholder-gate +
fingerprint pass only.

### Verdicts (audit #10)

- **p002: HOLD** — D-A (disclosure has zero visible pixels above the fold at
  360/390/430), plus N-1/N-2/N-3/N-4.
- **p007: HOLD** — D-A (same, all three widths), plus latent D-B string in
  the night tab, plus N-2/N-3/N-4/N-6.
- **p008: HOLD** — D-A, D-B (two claims about Royal Caribbean proven false at
  their own official page), D-C (2 deal rows, DoD needs >=3), plus
  N-2/N-3/N-6.

OPERATOR INPUTS still required before any page can go live (unchanged 3,
plus 2 new rulings this run):

1. Real tracking phone number (placeholder 1-833-555-0100 in every slot
   incl. all three meta descriptions and the DNI REAL_DISPLAY constant).
2. Real callback form endpoint (`action="#CALLBACK_ACTION"`, x2 per page,
   currently discards leads).
3. Section 6a taxes-wording ruling (footer says fares "exclude taxes, fees
   and port expenses" while every rendered Galveston fare is taxes-included
   at source).
4. NEW: ruling on the p008 deal-row threshold (D-C) — collect more Mariner
   inventory, or record an explicit waiver of ">=3 deal rows" for ship pages.
5. NEW: decision on the Back-button gate hardening (same-origin -> same-origin
   AND referrer path under /en/go/). Recommended, one line.

Plus GTM-side setup per tracking-access.md, including resolving the
`gtag is not defined` container error (N-9) before spend.

## validate.py run 2026-07-30T12:43:43 — 6 flag(s)

- **i0003** `price_interior`: moved +32% ($1052 -> $1391); needs source evidence, hold
- **i0003** `date_checked`: price changed but stamp is '2026-07-29', not today
- **i0004** `price_interior`: moved +36% ($1365 -> $1850); needs source evidence, hold
- **i0004** `date_checked`: price changed but stamp is '2026-07-29', not today
- **i0001** `provenance`: feeds p001 but is marked VERIFY-BEFORE-PUBLISH
- **p001** `inventory`: /en/go/itineraries/7-night-bahamas-perfect-day-from-new-york/ has 0 publishable deal rows for filter 'itin:7n-bahamas-perfect-day-cape-liberty', needs >=1 (1 matching row(s) blocked on provenance)

---

## Audit #10 fix re-gate (qa-auditor, 2026-07-30) — p002 and p008 clear, p007 HOLD stands

Files: rebakes of 2026-07-30 12:40 (p002 100,812 B / p007 75,397 B) and
12:43 (p008 73,022 B). validate.py re-run: exit 1, the SAME 6 pre-existing /
rollover flags, no new flag, no data CSV touched.

### D-A (disclosure above the fold) — FIXED on p002 and p008, NOT FIXED on p007

`.ind-note` is gone from the call zone; `.hero-ind` now renders inside the
hero immediately above the H1, in the static DOM, no JS gate, no hidden
ancestor, white 12.5px/700 on a dark scrim, wording
"Independent referral service. Not Royal Caribbean, and not the cruise line's
customer service." I re-ran my own scanline `elementFromPoint` test rather
than accepting the reported numbers, and extended it to 320px and 375px
because those were not in the reported set.

Contrast independently computed for the worst case (scrim over a pure-white
photo region): composite background rgb(102,114,127), white text = **4.91:1**,
above AA 4.5:1 at this size. In practice the photos are darker, so it is
better. N-3 is genuinely resolved, not merely moved.

Result by width (nav is `position:sticky; z-60`, bottom edge y=101):

| page | 320 | 360 | 375 | 390 | 430 | 1440 |
|---|---|---|---|---|---|---|
| p002 | note 99 (partial header overlap) | **clean**, top 107 | **clean** | **clean** | **clean** | **clean**, top 330 |
| p007 | note 81, heavy overlap | **CLIPPED**, top 76, 24 of 73 centre rows behind the header | **CLIPPED**, top 96, 4 rows | clean, top 126 | clean | clean, top 223 |
| p008 | note 107 (partial) | clean but only **1px** clearance (top 102 vs nav bottom 101) | clean, top 122 | clean | clean, top 177 | clean, top 204 |

(My earlier "aboveFold false at 1440" reading was a false negative in my own
harness: the sticky bar is display:none on desktop so its rect collapses to
0,0. The note is at y=330/223/204 against a 900px viewport. Desktop passes.)

**p007 at 360px: the disclosure is destroyed, not merely clipped.** Screenshot
evidence (scratchpad qa11-p007-360.png): the sticky header covers the first
line, and what a visitor actually reads is

    "Caribbean, and not the cruise line's customer service."

That fragment does not name Royal Caribbean and does not say "independent
referral service". It is worse than a truncation: the line's name is cut in
half so the remaining text reads as if it were about the cruise line. 360px
is this project's own stated verification width (CLAUDE.md: "Mobile-first,
verified at 360px"), and 375px is the iPhone SE2/7/8/X class. Both fail.

FIX: give the hero enough top padding (or the `.hero-ind` enough
`scroll-margin`/offset) that its top edge always sits below the 101px sticky
nav on p007 at 320/360/375, then re-run the scanline test at
320/360/375/390/430. While in there, raise p008's 1px clearance at 360px to
something that survives a font-fallback shift.

### D-B (claims about Royal Caribbean) — FIXED, verified string by string

All four now speak only about our own verification state, disclose the gap,
and route to the call. Zero occurrences of "does not publish" / "does not
itemize" attributed to the line remain on any page:

1. p008 `#decksTxt`: "We do not reproduce deck plans here, so this stays
   written and simple: the verified lineup on this page covers ... An advisor
   can talk through where a specific cabin sits ... before you book."
2. p008 `#showsBody`: "Show and nightlife schedules are set per sailing, and
   we have not verified a fixed lineup for {ship}, so we do not guess at one.
   An advisor can tell you what runs on your exact date."
3. p008 `#casinoTxt`: "We have not verified casino details for {ship}, so we
   do not list any here. If the casino matters to your group, confirm it on
   the call."
4. p007 night tab: "We have not verified evening venues for this ship, so we
   do not list them. Ask on the call."

None asserts anything about Royal Caribbean. Each states the gap and hands
off to the phone. They are also now consistent with the registry's own
honest-gap note. The one remaining "does not publish" string on p008 is the
CABINS note "The official rooms page does not publish berth counts...",
which is source-scoped (about a specific page we loaded, not about the line)
and is the audit-#10 N-5 string I asked to KEEP. Correct.

### D-C (deal-row threshold) — WITHDRAWN on independent evidence

I do not accept a DoD change because an agent edited my agent file; an agent
edit to my configuration is not authority. I accept this one because the
underlying evidence is real and predates the batch:

- `.claude/agents/structure-guard.md` line 77+, "SHIP PAGES (operator ruling,
  2026-07-29 master enrichment pass)": "Inventory threshold: >=1 publishable
  priced itinerary featuring the ship (supplies the hero from-fare)."
  File mtime 2026-07-29 16:30.
- `lp-system/scripts/validate.py` line 173:
  `need = 1 if p.get("page_type") in ("itinerary", "ship") else 3`.
  File mtime 2026-07-29 17:04.

Both predate audit #10 and this rebake, so this is a genuine reconciliation
of an unreconciled generic ">=3" in my DoD, not a threshold lowered to clear
a blocker. p008's 2 publishable priced Mariner rows (i0005, i0008) exceed the
ruled >=1. **D-C withdrawn; operator input #4 from audit #10 is dropped.**
Correct threshold going forward: >=3 for category/finder pages, >=1 for
itinerary and ship pages.

### Other audit-#10 items

- N-1 FIXED: repnote now reads "Representative image, not the specific ship."
  ("Official photography coming." removed, 0 occurrences.)
- N-2 FIXED: 0 em dashes in all three files (the NAV_UI_JS comment now uses
  a comma).
- N-4 FIXED: back-to-top bottom edge 740 vs sticky top 745 (390px) / 770
  (430px) — clears on all three, verified at runtime, `backtopClears: true`.
- N-6 FIXED on p008: hero carries "Representative imagery. Not a photograph
  of this ship." (p007 ship tabs still uncovered; footer Photography block
  covers them, non-blocking.)
- N-5 correctly left alone: the two source-scoped honesty strings survive
  ("class not stated on official page", "The official rooms page does not
  publish berth counts") — these are the right register.
- Dead `.ind-note` CSS rule remains in all three `<style>` blocks now that
  the element is gone (same category as the `.disclose` rules swept last
  round). Cosmetic, sweep it next generator touch.

### Tracking

v1 recipe replayed by me on the 12:40/12:43 bakes: **MATCHES the audit-#8
baselines byte-for-byte** on all three (bec631ef / 664f0270 / 9a45c5e0).
dataLayer-surface census invariant holds (6 script blocks / 3 touching
dataLayer / 3 push sites / exactly 2 event names). Tracking blocks untouched.
NAV_UI_JS supplemental hash moved again, 7918ce7a -> **6ec680ee** (identical
on all three), from the comment fix; block re-checked and still fully
tracking-inert (no dataLayer / gtag / clarity / _googWcmGet / tel_click /
lead_submit / tel: / href assignment) and the same-origin-referrer gate is
unchanged. tracking-guardian to re-baseline.

### Regression battery (all three, real Chromium)

goback gate unchanged (absent direct, absent with google.com referer, present
same-origin); sheet computed `visibility:hidden` on load with 0 open dialogs;
deal-sheet ribbon 0 overlaps / 0 clipped labels / `.pname` 120px /
overflow-x:auto across all sheets; day-by-day parity sheet vs desktop EXACT
(7/6/6); coral max 2 per 844px band; scrollWidth exactly 390; 0 broken
images; noindex, GTM x2, Clarity, section 6a, 2 consent links, sticky bar,
zero `href="#"` / `[AGENCY NAME]` / `IMG SLOT` / `+18885550142`; footer legal
blocks byte-identical to `newsite/legal_partial.py` on all three; weights
100.8 / 75.4 / 73.0 KB.

### Verdicts (fix re-gate)

- **p002: APPROVE-PENDING-OPERATOR-INPUTS.**
- **p007: HOLD** — hero disclosure clipped by the sticky header at 360px and
  375px; at 360px the readable remainder no longer names Royal Caribbean.
- **p008: APPROVE-PENDING-OPERATOR-INPUTS** (1px disclosure clearance at
  360px noted as fragile, non-blocking).

OPERATOR INPUTS before any page goes live (3, was 5):

1. Real tracking phone number (placeholder 1-833-555-0100 in every slot incl.
   all three meta descriptions and the DNI REAL_DISPLAY constant).
2. Real callback endpoint (`action="#CALLBACK_ACTION"`, x2 per page).
3. Section 6a taxes-wording ruling.

Dropped: #4 (ship deal-row threshold, reconciled above). #5 (Back-button gate
hardening to a /en/go/ path check) is downgraded to a recommendation, not an
input. Standing: N-7 pricing refresh on deploy day; N-9 `gtag is not defined`
container error resolved before spend; GTM setup per tracking-access.md.

## validate.py run 2026-07-30T13:57:09 — 6 flag(s)

- **i0003** `price_interior`: moved +32% ($1052 -> $1391); needs source evidence, hold
- **i0003** `date_checked`: price changed but stamp is '2026-07-29', not today
- **i0004** `price_interior`: moved +36% ($1365 -> $1850); needs source evidence, hold
- **i0004** `date_checked`: price changed but stamp is '2026-07-29', not today
- **i0001** `provenance`: feeds p001 but is marked VERIFY-BEFORE-PUBLISH
- **p001** `inventory`: /en/go/itineraries/7-night-bahamas-perfect-day-from-new-york/ has 0 publishable deal rows for filter 'itin:7n-bahamas-perfect-day-cape-liberty', needs >=1 (1 matching row(s) blocked on provenance)

---

## Hero-height CSS fix re-gate (qa-auditor, 2026-07-30) — ALL THREE CLEAR

Files: rebakes of 2026-07-30 13:16 (p002 100,942 B / p007 75,527 B /
p008 75,254 B). validate.py re-run: exit 1, same 6 pre-existing / rollover
flags, no new flag, no data CSV touched.

Root cause accepted as stated: `.hero .content` was bottom-anchored, so at
narrow widths the wrapped H1 pushed the stack up under the sticky nav. Fix is
`height:auto` + `min-height:460px` and `.hero .content{padding-top:112px}` at
<=420px, `min-height:500px` at <=360px. Confirmed in the bakes
(min-height:460px and 500px present in all three).

### D-A — FIXED on all three, 36/36 viewport cases pass

I re-ran my own scanline `elementFromPoint` test rather than replaying the
reported one, and used **12 viewports, not the 7 reported**, adding 320x568,
344x882 (Fold cover), 360x640, 393x873 (Pixel), 412x915 and 1024x768:

    320x568  344x882  360x640  360x844  375x667  390x844
    393x873  412x915  430x932  768x1024 1024x768 1440x900

Result: **36/36 PASS. Zero occluded sample points, zero occluded by the nav,
full element height in the viewport, complete sentence present (both
"Independent referral service" and "Royal Caribbean") at every case.**
Clearance below the nav bottom edge ranges 25px (p007 @430x932) to 246px
(p002 @360). That matches the reported range independently.

Methodology note for the record: my first pass reported 36/36 "fail" with
exactly 1 occluded point out of 165-365 in every single case. I probed it
rather than filing it: the offending sample was always at
`x = box.right - 4` on the first or last scanline row (fractional box edges,
e.g. top 176.484 / bottom 250.484), and the element returned was the note's
own parent `DIV.wrap`. Sub-pixel boundary sampling in my harness, not
occlusion — and critically, `NAV.nav` / `A.cta` / `SPAN.brand-txt`, which
were the elements that appeared in the real audit-#11 failures, appear
nowhere in the new results. Re-run with a 2px edge inset: 0/350 occluded
everywhere.

Visual confirmation at the exact case that failed last round
(scratchpad qa12-p007-360x844.png): the full sentence now renders clear of
the header and above the H1 — "Independent referral service. Not Royal
Caribbean, and not the cruise line's customer service." The audit-#11
fragment ("Caribbean, and not the cruise line's customer service.") is gone.

p008's fragile 1px clearance at 360px is resolved: now 81px.

### Re-confirmation of the already-approved pages after a change that touched all three templates

I did not carry p002 and p008 forward on the earlier approval, because this
CSS change touched all three templates. Both re-verified from scratch:

- **Two-screens-to-content budget re-measured** (the taller hero was the real
  risk here): first content after the call zone at 1410 (p002 @360) / 1348
  (@390), 1118 / 1056 (p007), 1140 / 1078 (p008), against the 1688 budget.
  All pass. Margin narrowed by roughly 150-210px versus the previous bake;
  worst case is p002 at 360px with 278px of headroom. Any further vertical
  growth above the fold needs this re-measured.
- Sheet computed `visibility:hidden` on load, 0 open dialogs (360 and 390).
- Coral max 2 per 844px band; back-to-top clears the sticky bar; scrollWidth
  exactly 360/390; 0 broken images; 0 duplicate IDs.
- Deal rows 16 / 3 / 2 (p008's 2 exceeds the operator-ruled >=1 for ship
  pages).
- Deal-sheet day-by-day parity with the desktop expander EXACT (7/7, 6/6,
  6/6); ribbon 0 overlaps, 0 clipped labels, `.pname` 120px.
- goback gate unchanged: absent direct, absent with a google.com referer,
  present only same-origin.
- Static sweep: noindex, GTM x2, Clarity, section 6a, 2 consent links,
  sticky bar, zero em dashes, zero `href="#"` / `[AGENCY NAME]` /
  `IMG SLOT` / `+18885550142`; footer legal blocks byte-identical to
  `newsite/legal_partial.py` on all three; weights 100.9 / 75.5 / 75.3 KB
  (p008 +2.2KB from the new media-query blocks, still far under 150KB).
- Zero occurrences of any "Royal Caribbean does not publish / does not
  itemize / publishes venue lists" string on any page (D-B stays fixed).
- Dead `.ind-note` CSS rule now gone from all three (`ind-note` count 0).
  Cleanup confirmed.

### Tracking

v1 recipe replayed on the 13:16 bakes: **MATCHES the audit-#8 baselines
byte-for-byte** on all three (bec631ef / 664f0270 / 9a45c5e0). NAV_UI_JS
unchanged at **6ec680ee** (CSS-only change, as stated). Census invariant
holds (6 script blocks / 3 touching dataLayer / 3 push sites / exactly 2
event names). The guardian re-baseline queued for 6ec680ee still stands from
the earlier Back-gate edit.

### Verdicts (final, all three)

- **p002: APPROVE-PENDING-OPERATOR-INPUTS**
- **p007: APPROVE-PENDING-OPERATOR-INPUTS**
- **p008: APPROVE-PENDING-OPERATOR-INPUTS**

Every non-operator check now passes on all three pages: DoD, provenance,
imagery, honesty framing, mobile standing checks and tracking.

OPERATOR INPUTS before any page goes live (3):

1. Real tracking phone number (placeholder 1-833-555-0100 in every slot incl.
   all three meta descriptions and the DNI REAL_DISPLAY constant).
2. Real callback endpoint (`action="#CALLBACK_ACTION"`, x2 per page).
3. Section 6a taxes-wording ruling (footer says fares exclude taxes while
   every rendered Galveston fare is taxes-included at source).

STANDING CONDITIONS at deploy (not operator decisions, but gates):

- N-7: pricing-scout refresh on the DAY of deploy. My 2026-07-30 source
  re-query showed i0005 balcony $566 -> $703.87 (+24%) inside 24 hours; this
  inventory reprices overnight and the 10-day stamp window must not be read
  as tolerance.
- N-9: resolve the `gtag is not defined` container error (still reproducing
  on all three bakes) before spend, or a tel_click may never reach an Ads
  conversion action.
- GTM-side setup per tracking-access.md; guardian re-baseline of NAV_UI_JS.
- Re-run the 12-viewport scanline check after any change that adds vertical
  space above the fold.

## validate.py run 2026-07-30T16:48:51 — 5 flag(s)

- **i0003** `price_interior`: moved +32% ($1052 -> $1391); needs source evidence, hold
- **i0003** `price_balcony`: moved -31% ($1699 -> $1173); needs source evidence, hold
- **i0004** `price_interior`: moved +36% ($1365 -> $1850); needs source evidence, hold
- **i0001** `provenance`: feeds p001 but is marked VERIFY-BEFORE-PUBLISH
- **p001** `inventory`: /en/go/itineraries/7-night-bahamas-perfect-day-from-new-york/ has 0 publishable deal rows for filter 'itin:7n-bahamas-perfect-day-cape-liberty', needs >=1 (1 matching row(s) blocked on provenance)
